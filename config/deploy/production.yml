# Production UI configuration
servers:
  web:
    - 5.6.7.8 # Production EC2 IP

proxy:
  ssl: true
  host: app.yourdomain.com
  app_port: 80

builder:
  args:
    VITE_API_URL: https://api.yourdomain.com
